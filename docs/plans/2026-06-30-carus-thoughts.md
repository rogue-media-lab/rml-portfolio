## The welcome action is something to consider. In the paper design the welcome is step 3 of onboarding and gives little information. Step 2 of onboarding is the vehicle. Does it include a way to upload a image? At work we use R.O Writer. Through DVI Suite we take five total pictures, one for each side and the dash. Should there be a way to add more than one image?  Don't think you need that option in onboarding though. Just the one noce pic for onboarding. What are the recomendations for the welcome action. 

## The AI features are still broken and clunky. They still are nothing like what I have through Discord using Hermes. I have yet to get the VIN decoder work from the image. I wonder if we should forgo the VinVisionService and just pass the image direct in the call. I wonder if the image is being taken by active record and s3 before AI gets a chance and so it returns nothing. Again just a guess, but it is not working. This is a real problem. This one features is used daily by Waypoint Discord. For CarUs the VIN is used in several features and AI Enhancements. Putting a VIN in manually is doable and should be allowed, but reading it from the image makes life so much easier. The AI chat still has problems. I love that you can create threads. Those threads do not have names, which makes things difficult. The list of chats is long seeing we are testing. It would be nice to have a way to delete them or be able to set it so only the chats for that day show, or hide after a few days. Only show the most recent? Also a chat should reference a customer vehicle when relevant. This would allow the chat to show when looking up the customer or vehicle. This still needs work, but solid start. 

## As for booking. I have always thought we would use a calendar here. This would allow the customer to select slots that are open. The shop manager could then have the calendar of appointments to assign to techs. This should make managing this across customers and techs much easier.

## For shop managers there should be a way to create estimates. This should be made easier with the bookings. Customer could say they want front brakes. The shop manager could easily look up those parts and labor times like they always have, but be able to add those line items, auto calculate tax and shop supplies, travel fee, diag fee and generate the estimate for the customer, sending it for approval. On approval then the shop manager could assign it to tech. The tech would get the customer, vehicle, jobs to do, address, check list of parts to pick, and where to get the parts.

## The manual needs some thought. I do not think we need the entire manual specific to that vehicle, but such constant info like fluid types, weights and capacities. Common torques like drain plug, filter housing torque. Windshield wiper sizes. Light bulbs number. Filters number. Reset proceure for maintenance. What the dash lights icons mean. Tires psi. Stuff like that.

## Search and lookup is a issue. Customers should belong to a shop. The vehicle specs should be global. A customer may have a honda accord, and that honda accord is thiers, but the honda accord specs for that year should be available to any and every tech. A tech in Rock Hill shop should not see maintenance histroy for a customer in a Dallas shop, but should see engine size for the honda and how many quarts of oil. The vehicle is universal or global. The customer data regarding there vehicle should be secure. Ask if this does not make sense.
